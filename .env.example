# Copie para `.env` na mesma pasta (ou deixe o repositório criar `.env` local ignorado pelo git).
# Valores só para desenvolvimento local.

ADMIN_PASSWORD=AdminTeste@local
JWT_SECRET=dev-jwt-local-troque-em-producao
